# Skins por juego

Fuente: diagnóstico de `skin-designer` (`.claude/agents/skin-designer.md`) cruzado con
`lib/game-engines.ts` (`GAME_ENGINES`) y la tabla `games` de Supabase. Actualizado 2026-09-08.

Un skin cuenta como **implementado** solo si el componente del motor lo aplica de verdad al pintar
(paleta/constantes seleccionables en el código), no si solo está planeado en un spec.

| id (`games.id`) | Título             | Componente                                     | `neon` | `retro` | `clasico` (default)                                                                                                                                           | Última revisión | Notas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------- | ------------------ | ---------------------------------------------- | ------ | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `asteroides`    | ASTEROIDES         | `components/games/asteroids/AsteroidsGame.tsx` | ✅     | ✅      | ✅ (`useState<SkinId>("clasico")`, línea 381; paleta idéntica a los colores previos: `#fff` nave/asteroide/bala, `#0ff` power-up/hud accent, fondo `#000`)    | 2026-09-08      | 3 de 3 skins implementados (spec `specs/11-asteroids-skins.md`, rama `spec-11-asteroids-skins`, Status: Implemented). `SkinId`/`SKIN_PALETTES` como constantes de módulo (líneas 21-71); `Bullet`/`Asteroid`/`PowerUp`/`Ship`/`Particle` reciben `palette` en `draw()`; selector `<select>` overlay esquina inferior derecha (líneas 680-702); persistencia `localStorage` clave `asteroids-skin` con `try/catch` (líneas 392-411). `neon` ancla la nave en `#f5ff00` (= `games.color` "yellow" del catálogo, confirmado en Supabase); resto de acentos toma `--magenta`/`--cyan`/`--green` de `app/globals.css`. `retro` es monocromático ámbar `#ffb000` (resuelve el riesgo de contraste señalado en la revisión anterior sobre el trazo fino de 1.5px de nave/asteroides). Sin riesgos de contraste nuevos contra `--bg`/`--bg-2`/`--bg-3` (`#0a0a0f`/`#0f0f18`/`#15151f`): las tres paletas mantienen fondo de canvas `#000` y todos los tonos jugables son de alta luminosidad.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `bloque-buster` | BLOQUE BUSTER      | `components/games/arkanoid/ArkanoidGame.tsx`   | ✅     | ✅      | ✅ (`useState<SkinId>("clasico")`; `spriteFilter: "none"` deja el spritesheet con sus colores originales, sin ningún cambio visual respecto al código previo) | 2026-09-08      | 3 de 3 skins implementados en esta sesión (sin spec — implementación directa por `skin-designer` según su contrato actualizado). Diferencia clave frente a Asteroids/Snake: Arkanoid pinta bloques/paleta/bola/explosiones recortando un único spritesheet PNG (`spritesheet-breakout.png`), no con `fillStyle`/`strokeStyle` planos, así que la paleta no reemplaza colores por elemento — aplica `ctx.filter` (CSS canvas filter) sobre cada `drawImage` de sprite (`drawSprite`/`drawBlockSprite`/`drawFrame`), más `bg`/`hudText` como color plano para el fondo del canvas y el texto de score/nivel/vidas. `neon` usa `saturate(2.4) brightness(1.2) drop-shadow(0 0 4px rgba(0,245,255,0.55))` con `hudText: #00f5ff` (= `games.color` "cyan" de `bloque-buster`, confirmado en Supabase = `var(--cyan)` de `app/globals.css`). `retro` usa `grayscale(1) sepia(1) saturate(3.2) hue-rotate(-28deg) brightness(0.95) contrast(1.05)` con `hudText: #ffb000` (mismo ámbar retro que Asteroids/Snake, por consistencia de plataforma). `SKIN_STORAGE_KEY = "arkanoid-skin"`, mismo patrón `skinRef`/`localStorage` con `try/catch`/`<select>` overlay inferior derecho. Sin riesgos de contraste detectados: fondo de canvas se mantiene `#000` en los tres skins, y el filtro de `retro` conserva brillo (`brightness(0.95)`) suficiente para que paleta/bola/bloques no se acerquen a la luminosidad del fondo. Pendiente de verificación visual en navegador por el usuario (este agente no tiene herramientas de navegador) — en particular confirmar que `ctx.filter` con `drop-shadow` no degrade el rendimiento del loop y que el efecto se vea bien en los 5 niveles/colores de bloque. |
| `serpentina`    | SERPENTINA (Snake) | `components/games/snake/SnakeGame.tsx`         | ✅     | ✅      | ✅ (`useState<SkinId>("clasico")`; paleta idéntica a los colores previos: cabeza `#8dffc2`, cuerpo `#3ddc84`, HUD `#fff`, fondo `#000`)                       | 2026-09-08      | Diagnóstico inicial: 0 de 3 (solo constantes fijas `"#8dffc2"`/`"#3ddc84"`/`"#fff"`/`"#000"` en el `draw()` original). Implementado directamente en esta sesión generalizando el patrón de `AsteroidsGame.tsx`: `SkinId`/`SkinPalette`/`SKIN_PALETTES` como constantes de módulo, `skinRef` leído dentro de `draw()`, persistencia `localStorage` clave `snake-skin` con `try/catch` en montaje y en `handleSkinChange`, selector `<select>` overlay esquina inferior derecha. `neon` ancla la cabeza en `#00ff88` (= `games.color` "green" del catálogo, confirmado en Supabase); cuerpo `#00cc6f` más oscuro para diferenciar cabeza/cuerpo; acento HUD "Nivel" en `#ff006e` (magenta). `retro` es monocromático ámbar (`#ffb000` cabeza / `#b37c00` cuerpo), mismo fósforo retro ya usado en Asteroids para mantener identidad de plataforma. Los sprites de fruta (`public/games/snake/fruits.png`, `FRUIT_SPRITES`) se mantienen idénticos en los 3 skins — son imágenes fijas, no colores repaletizables; no se tocó `FRUITS_SRC` ni la carga de la imagen. Sin riesgos de contraste: las tres paletas mantienen fondo de canvas `#000` (igual que `crt-screen`) y todos los tonos de cabeza/cuerpo/HUD son de luminosidad media-alta, lejos de `--bg`/`--bg-2`/`--bg-3` (`#0a0a0f`/`#0f0f18`/`#15151f`).                                                                                                                                                                                                                                                                                                                                                                                      |

## Sin motor todavía

(sin entradas registradas por este agente todavía)
